Phase 3 — Feature-based test organization

Goals:
- Split tests into files by feature (upload, download, search, auth)
- Create module-per-feature in `tests/e2e` and `tests/integration`
- Add smoke tests after each move

This is a plan + scaffolding PR; actual file moves will follow in narrow PRs.

