apiVersion: v1
kind: Secret
metadata:
  name: just-storage-secrets
  namespace: just-storage
type: Opaque
stringData:
  # Placeholder values only. Do not commit real credentials in this template.
  # Production credentials must come from Vault/VSO or another secret manager.
  database-url: "postgresql://USER:PASSWORD@HOST:5432/just_storage"
  admin-token: "replace-with-secret-manager-reference"
