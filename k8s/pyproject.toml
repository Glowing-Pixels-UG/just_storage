[project]
name = "just-storage-k8s"
version = "0.1.0"
description = "Kubernetes deployment scripts for JustStorage"
requires-python = ">=3.9"
dependencies = [
    "kubernetes>=28.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

