name: Docker Build

on:
  push:
    branches: [main, master]
    tags: ["*"]
  pull_request:
  workflow_dispatch:

env:
  REGISTRY: registry.bk.glpx.pro
  IMAGE: registry.bk.glpx.pro/just-storage/just-storage

jobs:
  docker:
    uses: mukimovd/.github/.gitea/workflows/docker-ci.yml@main
    with:
      image-name: just-storage/just-storage
    secrets: inherit
