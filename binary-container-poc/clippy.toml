# Clippy configuration for binary-container-poc
# Based on 2025 best practices for Rust static analysis
#
# Note: Most Clippy configuration is done via command-line flags.
# This file is for specific lint configuration that can't be set via flags.

# Set the MSRV (Minimum Supported Rust Version) if needed
# msrv = "1.70.0"

# Configure specific lints
# Most lints are configured via command-line: cargo clippy -- -D warnings

